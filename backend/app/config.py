# Application Configuration & Environment Settings
# - Defines paths for document uploads (data/uploads) and vector storage (chroma_db)
# - Stores model specifications (Embedding model name, LLM model name/API keys)
# - Configures RAG hyperparameters (chunk size, chunk overlap, top_k retrieval)
